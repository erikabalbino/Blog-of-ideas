<h1  class="mb-4"> ✧ ✧ All Ideas ✧ ✧ </h1>
<div>
    <% @ideas.each do |idea| %>
        <%= link_to(
        idea.title,
        idea_path(idea.id),
        ) %>
        <br />
        <p><%= idea.description %></p>
        <hr>
    <% end %>
</div>